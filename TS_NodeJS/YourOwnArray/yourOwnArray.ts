const wishes : string[] = ["Helicopter"," Private Jet"," Cruise Ship"," $10,000,000 Private Island"];

for (let S = 0; S < wishes.length; S++);
{
    console.log(`I would like to own a ${wishes}.`);
}